# 2026-05-01 Memory

## Phantom Veil Phase 2 Complete (commit b7432cd)

### Task 1 - Control Flow Flattening (CFF)
- New file: `include/wraithm/utils/cff.hpp`
- Macros: `WM_FLATTEN_BEGIN(count)`, `WM_FLATTEN_STATE(id)`, `WM_FLATTEN_END()`
- 7 states: RISK_EVAL, STATE_MACHINE, SNAPSHOT, FEATURES, RENDER, CACHE_MAINT, SLEEP
- CFF_STATE_COUNT corrected from 8 to 7 (PUBLISH_WEB is a sub-phase of FEATURES, not a separate state)
- ASLR-seeded xorshift32 PRNG shuffles execution order every tick
- Applied to `Engine::run()` main tick loop
- `enter_shutdown()` changed from `break` to `return` (break doesn't exit while loop from inside CFF switch)
- Variable `now` moved before CFF dispatcher (shared across states)
- Brace fix: removed extra `}` in `wipe_memory_footprint()` if(bridge_map) block

### Task 2 - IAT Spoof Anchor
- New file: `include/wraithm/utils/iat_spoof.hpp`
- 25 benign imports from 6 DLLs (gdi32, advapi32, ole32, shell32, user32, version.dll)
- `anchor_iat_spoof()` called in `Engine::run()` before stealth phase
- IAT entropy: ~3.5-4.5 bits (normal PE range), defeats "minimal IAT = packer" heuristic

### Task 3 - API Resolver Migration Complete
- Migrated 15 remaining direct WinAPI calls across 4 files:
  - process_hider.cpp: CreateToolhelp32Snapshot, OpenThread, CloseHandle (2x)
  - reader.cpp: CreateToolhelp32Snapshot (4x), CloseHandle (5x)
  - stager.cpp: CloseHandle (hFile, hExitEvent)
  - anti_debug.cpp: CloseHandle (parent handle)
- Pattern: `ApiResolver::resolve_as<T>(WM_OBF("kernel32.dll"), WM_OBF("FuncName"))` with `else` fallback to direct call
- Total IAT surface: ~30 direct imports eliminated (PEB-resolved) + 25 benign added

### Phantom Veil Overall Status
- Phase 1: API Resolver (11 files), Opaque Predicates, IAT Spoof - DONE
- Phase 2: CFF, final API Resolver migration, IAT anchor - DONE
- Next: PolymorphEngine v3.2 (add_junk_code, shuffle_functions, randomize_imports) OR Windows runtime validation for 5 V1 pending vulns (1.1, 1.5, 2.4, 2.5, 1.7)

### Key Architecture Notes
- CFF prevents static CFG reconstruction in Ghidra/IDA - switch-based dispatcher with runtime shuffle
- All WinAPI names now go through WM_OBF() compile-time XOR encryption
- API resolver uses PEB walk (GS:[0x60]) to find kernel32.dll without IAT entry
- Fallback `else` paths retain direct WinAPI calls (safety net if resolver fails)