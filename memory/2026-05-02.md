# 2026-05-02 — WraithM v3.1 Stabilization Continuation

## Phase 3: VULN-2.5 TLS Implementation (IN PROGRESS)

### Objective
Implement in-memory TLS via Windows Schannel API to encrypt all control interface traffic.
- Self-signed X.509 cert generated in memory (zero disk writes)
- Windows Schannel handles TLS handshake and record layer
- Prevents LAN sniffing and MITM on same-network connections
- CertOpenStore(CERT_STORE_IN_MEMORY) + CertCreateSelfSignCertificate

### Files Being Created
- `include/wraithm/web/tls.hpp` — TLSContext + TLSSession classes
- `src/web/tls_context.cpp` — Schannel cert generation, credential management, session handling

### Files Being Modified
- `src/web/server.cpp` — Accept loop wraps connections in TLS before HTTP processing
- `include/wraithm/web/server.hpp` — Add TLSContext member, update comments
- `src/web/ssl_utils.cpp` — DELETE (dead stub, replaced by tls_context.cpp)
- `CMakeLists.txt` — Replace ssl_utils.cpp with tls_context.cpp, add secur32.lib
- `include/wraithm/core/engine.hpp` — Update stale comments (CivetWeb reference, SSL removed)

### Status
- VULN-1.1: ✅ ALREADY FULLY FIXED (all accesses use locks/copies)
- VULN-2.5: 🔧 IMPLEMENTING (Schannel TLS)

### Additional Cleanup (this session)
- Remove dead ssl_utils.cpp stub from CMakeLists and disk
- Update server.hpp stale comments (still says "6-digit code", "No SSL", "v1.2")
- Update engine.hpp stale comments (still references "CivetWeb HTTPS")
- Remove commented-out EngineConfig block in engine.hpp (dead code, moved to types.hpp)
# 2026-05-02 Memory Flush

## Phantom Project — TLS Integration (VULN-2.5 Fix)

### Status: IN PROGRESS
- CMakeLists.txt ✅ pushed (tls_context.cpp replaces ssl_utils.cpp, secur32.lib added)
- server.hpp ✅ pushed (tls_context_ member, use_tls config, cleanup method)
- tls.hpp ✅ pushed (TLSContext + TLSSession declarations)
- tls_context.cpp ✅ pushed (Schannel implementation, self-signed cert, zero disk)
- server.cpp ⚠️ TRUNCATED on GitHub (13KB instead of ~58KB) — needs re-push

### server.cpp Changes (TLS integration)
Key changes from old → new version:
1. Header: Added `#include "wraithm/web/tls.hpp"` + `#pragma comment(lib, "secur32.lib")`
2. HttpConnection: Added `std::unique_ptr<tls::TLSSession> tls_session` member
3. HttpConnection::send_response: TLS send if session is active, else plain socket
4. Constructor: Initialize TLS context if `config_.use_tls` is true
5. start(): Log protocol (HTTPS/HTTP), cert fingerprint; fallback port 8444
6. stop(): TLS context cleanup (`tls_context_->cleanup()`)
7. run() worker: TLS handshake before process_request
8. process_request(): TLS recv for header reading and body reading
9. Fallback port changed from 8081 → 8444 (more appropriate for HTTPS)

### Local File State
- Local `src/web/server.cpp` still has OLD version (no TLS)
- Need to write the updated version and push to GitHub
- The GitHub version is truncated — `push_files` should handle the full ~58KB

### Other Files Needed
- `src/web/ssl_utils.cpp` → DELETE from repo (replaced by tls_context.cpp)
- Engine.hpp stale comments about ssl_utils → already updated in server.hpp push

### Pending Action
Push the complete server.cpp via `github__push_files` (handles larger files better than create_or_update_file)
# 2026-05-02 — WraithM v3.1 Stabilization Continuation

## Phase 3: VULN-2.5 TLS Implementation (IN PROGRESS)

### Objective
Implement in-memory TLS via Windows Schannel API to encrypt all control interface traffic.
- Self-signed X.509 cert generated in memory (zero disk writes)
- Windows Schannel handles TLS handshake and record layer
- Prevents LAN sniffing and MITM on same-network connections
- CertOpenStore(CERT_STORE_IN_MEMORY) + CertCreateSelfSignCertificate

### Files Being Created
- `include/wraithm/web/tls.hpp` — TLSContext + TLSSession classes
- `src/web/tls_context.cpp` — Schannel cert generation, credential management, session handling

### Files Being Modified
- `src/web/server.cpp` — Accept loop wraps connections in TLS before HTTP processing
- `include/wraithm/web/server.hpp` — Add TLSContext member, update comments
- `src/web/ssl_utils.cpp` — DELETE (dead stub, replaced by tls_context.cpp)
- `CMakeLists.txt` — Replace ssl_utils.cpp with tls_context.cpp, add secur32.lib
- `include/wraithm/core/engine.hpp` — Update stale comments (CivetWeb reference, SSL removed)

### Status
- VULN-1.1: ✅ ALREADY FULLY FIXED (all accesses use locks/copies)
- VULN-2.5: 🔧 IMPLEMENTING (Schannel TLS)

### Additional Cleanup (this session)
- Remove dead ssl_utils.cpp stub from CMakeLists and disk
- Update server.hpp stale comments (still says "6-digit code", "No SSL", "v1.2")
- Update engine.hpp stale comments (still references "CivetWeb HTTPS")
- Remove commented-out EngineConfig block in engine.hpp (dead code, moved to types.hpp)

## Auditoria de Pré-Voo Completa (WraithM v3.1)

### ✅ PASSOU — Zero Issues

| Área | Status | Detalhe |
|---|---|---|
| **Orphan Symbols** | ✅ ZERO | 17/17 métodos definidos, 4/4 inline, 33/33 PhoneCommand mapeados |
| **Route Dispatch** | ✅ ZERO dead-ends | 7 rotas → 7 handlers, todos alcançáveis |
| **SSL órfão** | ✅ Removido | ssl_utils.cpp deletado, zero referências |
| **Deadlock Risk** | ✅ ZERO | data_mutex_ liberado antes de I/O em todos os handlers |
| **HTML Safety** | ✅ ZERO XSS | Zero innerHTML, tudo via textContent |
| **TLS Buffer Bounds** | ✅ | 16KB buffer máximo com stream_sizes_ |
| **PEB Walker** | ✅ | InMemoryOrderModuleList + offset -0x10 + SEH protection |
| **FNV-1a Constants** | ✅ | 2166136261 / 16777619 corretos |
| **IAT Spoof** | ✅ | 25 benign imports de gdiplus/advapi32/ole32/shell32 |
| **Constant-Time Auth** | ✅ CORRIGIDO | XOR bitwise com padding zero para Bearer header |
| **CORS Completo** | ✅ CORRIGIDO | Adicionados Methods, Headers, Max-Age headers |
| **CFF default case** | ✅ CORRIGIDO | Adicionado default: break com forensic cleanup |
| **Stale Comments** | ✅ CORRIGIDO | "CivetWeb" → "Schannel TLS", porta 8080 → 8443 |

### 🟡 CORRIGIDO NESTE COMMIT (6 Bugs)

| Bug | Severidade | Correção |
|---|---|---|
| **#1 Race Condition em verify_auth()** | 🟡 Médio | data_mutex_ protege lockout_until + XOR com padding |
| **#2 CORS Headers Incompletos** | 🟡 Médio | Adicionados Methods, Headers, Max-Age |
| **#3 CFF sem default case** | 🟡 Médio | default: break com forensic cleanup comment |
| **#4 Stale comment "CivetWeb"** | 🟢 Baixo | Atualizado para "Schannel TLS" |
| **#5 Porta default 8080** | 🟡 Médio | Corrigido para 8443 em engine.hpp e types.hpp |
| **#6 Bearer auth não era constant-time** | 🟡 Médio | Implementado XOR bitwise para Bearer header também |

### 🔴 DEFERIDO — Próxima Sessão (Windows Build Phase)

| Issue | Severidade | Razão do Defer | Arquivo(s) |
|---|---|---|---|
| **TLS handshake sem timeout dedicado** | 🟡 Médio | Requer mudança em tls_context.cpp — adicionar `GetTickCount64()` no loop de handshake com limite de 5000ms | `src/web/tls_context.cpp` |
| **Impl destructor sem memory zeroing** | 🟡 Médio | Requer `SecureZeroMemory` para buffers sensíveis antes do free no destructor de `Engine::Impl` | `src/core/engine.cpp` |
| **wipe_memory_footprint sem SecureZeroMemory** | 🟡 Médio | Compiler pode otimizar `memset` para zero — trocar por `SecureZeroMemory` ou volatile write loop | `src/core/engine.cpp` |
| **47 WinAPI string leaks** | 🔴 Alto | 47 strings sensíveis em texto claro ("VirtualAlloc", "WriteProcessMemory", etc.) fora da macro `WM_OBF`. Afeta stealth/*.cpp principalmente. Requer refatoração extensiva com WM_OBF wrapping. | `src/stealth/anti_debug.cpp`, `src/stealth/pe_wiper.cpp`, `src/stealth/vfs_hook.cpp`, `src/stealth/permission_spoof.cpp`, `src/core/engine.cpp` (lines 1589, 1593, etc.) |

### 🔴 PENDING FIXES (Next Session Checklist)

1. **tls_context.cpp handshake timeout**: Adicionar `constexpr DWORD TLS_HANDSHAKE_TIMEOUT_MS = 5000;` antes do namespace, e no loop while do handshake adicionar:
   ```cpp
   DWORD handshake_start = GetTickCount();
   while (...) {
       if (GetTickCount() - handshake_start > TLS_HANDSHAKE_TIMEOUT_MS) {
           WM_ERROR("tls", "Handshake timeout exceeded");
           return false;
       }
       // ... existing recv/send logic
   }
   ```

2. **engine.cpp SecureZeroMemory**: No `wipe_memory_footprint()`, adicionar:
   ```cpp
   // Zero sensitive memory before freeing
   if (impl_->risk_bridge) {
       // Zero bridge buffer if accessible
   }
   // Zero any snapshot data, config tokens, etc.
   SecureZeroMemory(&impl_->bridge_config, sizeof(impl_->bridge_config));
   ```

3. **47 String Leaks**: Documentação detalhada no Windows Build Phase — arquivos prioritários:
   - `src/stealth/anti_debug.cpp` — strings de WinAPI para detecção de debugger
   - `src/stealth/pe_wiper.cpp` — strings de VirtualAlloc/WriteProcessMemory
   - `src/stealth/vfs_hook.cpp` — strings de kernel32.dll/CreateFileW
   - `src/stealth/permission_spoof.cpp` — strings de OpenProcess/VirtualProtect
   - `src/core/engine.cpp` — strings em ApiResolver calls que escaparam WM_OBF

### Commit Status
- **Commit atual**: `c0db635` (main)
- **Status GitHub**: ✅ Sincronizado
- **Próximo passo**: Windows Build (agente Windows) com estas correções aplicadas

## Local File State
- Local `src/web/server.cpp` still has OLD version (no TLS)
- Need to write the updated version and push to GitHub
- The GitHub version is truncated — `push_files` should handle the full ~58KB

### Other Files Needed
- `src/web/ssl_utils.cpp` → DELETE from repo (replaced by tls_context.cpp)
- Engine.hpp stale comments about ssl_utils → already updated in server.hpp push

### Pending Action
Push the complete server.cpp via `github__push_files` (handles larger files better than create_or_update_file)
