# Memory Flush — 2026-05-03 (final)

## LuaEduca — Tudo Concluído

### P0s ✅
- Rate limiting Postgres (migration APLICADA)
- n8n-teste hardening, CORS cleanup, health-check

### P1s ✅
- n8n-proxy refatorado (1405→7 módulos)
- Circuit breaker + retry com backoff
- HMAC auth delegation (proxy→n8n)
- Auth duplicada n8n (workflow PRO atualizado com HMAC verify)

### P2s ✅ (desta rodada)
- **systemd tunnel-health-check** — system service rodando, todos 3 checks OK
  - Script adaptado: fallback HTTP quando docker socket inacessível
  - Service: /etc/systemd/system/luaeduca-tunnel-health.service
- **useChatSend refatorado** — 743→4 hooks (556+69+55+103=783 linhas, mas modulares)
  - useStreamingDelta.ts — RAF buffering
  - useImageUpload.ts — upload via Edge Function
  - useTokenCreation.ts — submit-doubt token creation
  - useChatSend.ts — composição + webhook
  - Build passa, interface pública inalterada

### Commits desta sessão (9 total)
1. b61fddd — refactor(n8n-proxy): modularize monolith
2. 41a41d7 — feat(n8n-proxy): circuit breaker + retry
3. ba81a13 — feat(n8n-proxy): HMAC trust delegation
4. 25485f8 — chore: N8N_PROXY_HMAC_KEY to n8n env
5. 4da9d7f — feat(n8n): Docker healthcheck
6. e9015e5 — fix(tunnel-health): HTTP fallback + system service
7. 870cf20 — refactor(chat): extract useChatSend into 4 hooks

### Pendências restantes
- Dividir workflow PRO (47 nodes) em sub-workflows
- SSE parser compartilhado frontend/backend (parcialmente feito)
- Atualizar typeVersions + error handling nos nodes n8n
- Refatorar homepage-chat-proxy (menor prioridade)